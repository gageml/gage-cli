# `gage task list` command

    >>> run("gage task list --help")  # +diff
    Show available tasks
    ⤶
    Usage: gage task list [OPTIONS]
    ⤶
    Options:
      -p, --path <PATH>  Path to find tasks
      -h, --help         Print help
