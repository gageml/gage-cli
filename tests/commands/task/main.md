# `gage task` command

    >>> run("gage task --help")  # +diff
    Manage tasks
    ⤶
    Usage: gage task <COMMAND>
    ⤶
    Commands:
      eval  Evaluate tasks
      info  Show task info
      list  Show available tasks
      run   Run a task
      help  Print this message or the help of the given subcommand(s)
    ⤶
    Options:
      -h, --help  Print help
