# `gage profile use` command

    >>> run("gage profile use --help")  # +diff
    Use a profile
    ⤶
    Usage: gage profile use <PROFILE>
    ⤶
    Arguments:
      <PROFILE>  Profile to apply
    ⤶
    Options:
      -h, --help  Print help
